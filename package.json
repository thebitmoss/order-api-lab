{
  "name": "order-api-lab",
  "version": "1.0.0",
  "description": "Learning project for an order API with product inventory.",
  "main": "src/index.js",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "test": "node -e \"require('node:fs').closeSync(require('node:fs').openSync('prisma/test.db','a'))\" && DATABASE_URL=\"file:./test.db\" prisma migrate deploy && DATABASE_URL=\"file:./test.db\" node --test --test-concurrency=1",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma migrate deploy",
    "prisma:studio": "prisma studio"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "dotenv": "^17.4.1",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "prisma": "^6.19.3",
    "supertest": "^7.2.2"
  }
}
